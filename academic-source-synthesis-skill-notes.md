# 从学术多源综合助手看 Skill 设计：开发学习笔记

> 分析对象：`academic-source-synthesis.zip`  
> 分析日期：2026-08-05  
> 目的：从一个完整 Skill 样例中总结结构设计、触发机制、证据工作流与可改进之处。

## 1. 样例概览

压缩包包含三个文件：

```text
academic-source-synthesis/
├── SKILL.md
├── agents/
│   └── openai.yaml
└── assets/
    └── icon.svg
```

这是一个纯指令型 Skill：没有脚本和参考资料，主要依靠 `SKILL.md` 约束模型完成多来源学术材料的解析、去重、共识判断、分歧分类与引用定位。

文件安全检查未发现路径穿越项或可执行文件；YAML 可以正常解析，SVG 也是有效 XML。

## 2. 值得学习的设计

### 2.1 触发描述覆盖了“能力 + 场景 + 用户说法”

`description` 同时说明了：

- 输入：PDF、Word、网页链接、被引文献列表；
- 核心动作：解析、去重、聚合、比较、溯源；
- 输出：带页码、章节或链接定位的综合报告；
- 使用场景：比较论文、提炼共同结论、寻找分歧；
- 自然语言触发词：如“综合这些论文”“给结论加上出处”。

这符合 Skill 元数据的关键原则：是否触发主要由名称和描述决定，因此“什么时候使用”应尽量写在 frontmatter，而不是藏在正文中。

### 2.2 把“共识”写成了可审计规则

样例没有把“多篇文章都这么说”直接等同于共识，而是设置了明确门槛：

1. 至少两个来源明确表达同一原子主张；
2. 来源彼此独立；
3. 研究对象、条件与概念口径兼容；
4. 已读材料中不存在被忽略的直接反例；
5. 每项判断都有精确定位。

其中最有价值的是“独立性校验”。同一论文的预印本、正式版、新闻转载和评论文章即使表述一致，也不能被算成四份独立证据。这让工作流从普通摘要升级为证据综合。

### 2.3 原子主张是整个流程的中间表示

Skill 要求把每个来源拆成一次只表达一件事的主张，并记录主张类型、条件、立场、定位、转述关系和提取置信度。这个设计相当于先建立结构化的“主张台账”，再做跨来源合并。

它解决了几个常见问题：

- 避免把带限定条件的结果改写成普遍规律；
- 避免把实验观察和作者解释混成一层；
- 便于发现两个来源只是措辞相似，实际研究条件不同；
- 让最终报告中的每条结论可以回溯到证据。

### 2.4 对访问状态和定位能力进行了分层

样例区分了“全文可读、仅摘要、仅元数据、无法访问”，并规定仅元数据不能支撑内容主张。它也针对 PDF、Word、网页、扫描件、图表分别指定定位方法，尤其强调：

- PDF 文件页码和文内页码不一致时要分开记录；
- Word 没有稳定页码时不能虚构页码；
- OCR 中的关键数值需要回看原图；
- 参考文献表只证明“被引用”，不证明被引文献的实际结论。

这些规则很适合复用到法律材料、政策文件、尽调报告等强调可追溯性的 Skill 中。

### 2.5 输出结构与内部工作流对应

最终报告依次展示材料范围、来源清单、共同结论、一致但非独立的表述、单一来源补充、证据缺口和分歧。内部证据等级和外部报告栏目能够一一对应，减少了模型在输出阶段重新发明分类体系的风险。

## 3. 可以改进的地方

### 3.1 “全视角”与主题排除条款互相冲突

正文规定遇到“部门、政策或政治议题”就停止分析。这一限制过宽，也与“学术全视角报告”的定位冲突。政治学、公共政策、行政管理等本身就是正常的学术领域；仅凭主题拒绝，会产生大量误触发和误拒绝。

更稳妥的写法是保持内容中立：继续进行来源综合，但不替用户作政治说服、立场裁决或无证据推断；若确有产品安全边界，应把它写成精确的行为限制，而不是笼统排除整个学科。

### 3.2 `SKILL.md` 承担了过多细节

正文约 197 行，尚未超过常见的 500 行建议上限，但来源定位规则、主张台账字段、分歧分类、输出模板和审计清单都集中在一个文件里。触发后，模型每次都需要加载全部内容，即使用户只要求比较两篇短论文。

可以采用渐进披露：

```text
academic-source-synthesis/
├── SKILL.md
└── references/
    ├── evidence-ledger.md
    ├── citation-locations.md
    └── report-template.md
```

`SKILL.md` 只保留核心原则、主流程和何时读取各参考文件；字段表、定位细则和长模板移入 `references/`。

### 3.3 缺少确定性的台账工具

当前 Skill 完全依赖模型在上下文中维护来源编号和原子主张。材料较多、需要分批处理时，编号漂移、重复主张和遗漏来源的风险会上升。

可以增加一个小型脚本，用 CSV 或 JSONL 维护来源清单与主张台账，并提供以下检查：

- 来源编号唯一；
- 每项共同结论至少关联两个独立来源；
- 引用定位非空；
- 访问状态为“仅元数据”的来源不能支撑主张；
- 同一研究的多个版本不能被重复计数。

这种需要机械一致性的环节适合低自由度脚本；对主张语义是否等价的判断则应继续留给模型。

### 3.4 界面元数据需要收紧

`agents/openai.yaml` 的 `short_description` 长达 213 个字符，几乎完整复制了 `SKILL.md` 的触发描述，不像面向界面的简短摘要；同时缺少推荐的 `default_prompt`。

可改为：

```yaml
interface:
  display_name: 学术多源共识核验
  short_description: 综合多篇学术来源，核验共同结论、分歧与出处
  default_prompt: 使用 @academic-source-synthesis 比较这些学术来源，并输出可追溯的共识与分歧报告。
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
```

界面名称也应与正文中文标题保持一致，而不是使用全小写英文，除非产品定位明确要求英文显示。

### 3.5 “分歧统一置于文末”不应成为硬规则

把分歧集中放在文末能保持主线清晰，但也可能让读者先看到一个过于确定的“共同结论”，直到最后才发现关键反例。更好的规则是：

- 共同结论条目中立即注明是否存在适用范围或反例；
- 文末再集中展开分歧类型和完整证据；
- 对足以改变结论解释的直接冲突，不应仅在附后章节出现。

### 3.6 缺少可操作的失败与容量策略

样例说“超过单次处理容量时按章节分批”，但没有定义分批后的持久化格式、合并键和终止条件。建议明确：

- 每批产出同一字段结构的主张台账；
- 以稳定来源编号和主张编号跨批次合并；
- 合并完成后再运行一次覆盖审计；
- 无法读取完整材料时，不使用“已覆盖全文”等表述。

## 4. 从这个样例提炼出的通用开发方法

### 4.1 先设计判定对象，再写提示词

这个 Skill 真正的核心不是“写一份综述”，而是定义了三个判定对象：来源、原子主张、来源关系。开发新的分析型 Skill 时，先确定需要稳定追踪的中间对象，比直接堆叠写作要求更有效。

### 4.2 根据错误代价设置自由度

- 语义归纳、条件兼容判断：允许模型灵活推理；
- 编号、字段完整性、引用数量：使用模板或脚本硬检查；
- 涉及外部访问：明确授权工具和“未读不得声称已读”的边界。

### 4.3 把反幻觉要求写成可验证条件

“不要幻觉”过于抽象；“仅元数据不能支撑主张”“Word 无稳定页码时不得生成页码”“共同结论至少两个独立来源”则可以在交付前逐项审计。优秀 Skill 会把价值要求转化为检查项。

### 4.4 元数据负责触发，正文负责执行

触发描述要覆盖用户可能提供的材料、任务动作和自然语言表达；正文则不必重复“何时使用”，而应说明一旦触发后怎样完成任务。界面摘要又是第三种文本，应面向人类快速浏览，不能直接复制触发描述。

### 4.5 先做最小闭环，再添加资源

这个样例的最小闭环已经成立：输入 → 来源清单 → 原子主张 → 去重与独立性 → 共识/分歧 → 审计 → 报告。下一步应优先补充能显著降低错误的台账脚本和按需参考文件，而不是增加更多泛化原则。

## 5. 一个更合理的迭代顺序

1. 删除或改写过宽的政策/政治主题排除条款。
2. 精简并补全 `agents/openai.yaml`。
3. 将定位规则和报告模板拆入 `references/`。
4. 增加来源—主张台账及机械校验脚本。
5. 用三类任务前向测试：两篇短论文、同一论文多版本、包含直接冲突的混合来源。
6. 检查测试中是否出现虚构定位、重复计数、遗漏反例和跨批次编号漂移。

## 6. 总结

`academic-source-synthesis` 的主要价值，在于把“综述多篇文献”细化成一个可追溯的证据工程流程。它对来源独立性、访问状态、原子主张和分歧类型的处理很扎实，是分析型 Skill 的好范本。

若要进一步达到稳定可复用的水平，重点不是继续扩写原则，而是压缩触发后必须加载的正文、改善界面元数据，并把编号、台账和覆盖审计交给确定性工具。这样既能降低上下文成本，也能减少长材料处理中的机械错误。
